p#notice = notice

p
  strong Title:
  = @question.title
p
  strong Description:
  = markup(@question.description).html_safe
p
  strong User:
  = @question.user.name

=> link_to 'Edit', edit_question_path(@question)
'|
=< link_to 'Back', questions_path

div
  = render partial: 'answers/answers', locals: { question: @question }
